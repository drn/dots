#!/bin/bash

#
# If available, prints out WiFi SSID currently connected to.
# If none available, prints the current hostname minux the domain.
#

ssid="$(airport -I | grep ' SSID' | sed 's/.*SSID: //')"
if [ -z "$ssid" ]; then
  echo "$(hostname -s)"
else
  echo "$ssid"
fi

