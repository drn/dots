#!/bin/bash

#
# If available, prints out WiFi SSID currently connected to.
#

ssid="$(airport -I | grep ' SSID' | sed 's/.*SSID: //')"
if [ -z "$ssid" ]; then
  echo "[ offline ]"
else
  echo "$ssid"
fi

