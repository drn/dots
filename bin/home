#!/bin/bash

#
# SSHs into desktop either locally or remotely based on
# the current network name.
#

if [ "$(ip)" == "$(dig +short ip.drn.dev)" ]; then
  ssh SanguineRane@192.168.50.200 $1
else
  ssh SanguineRane@ip.drn.dev $1
fi
