#!/bin/bash

#
# SSHs into desktop either locally or remotely based on
# the current network name.
#

if [ "$(ip)" == "$(dig +short $HOME_WAN)" ]; then
  ssh $HOME_USER@$HOME_LAN $1
else
  ssh $HOME_USER@$HOME_WAN $1
fi
